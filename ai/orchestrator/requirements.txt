# AI Orchestrator — Phase 19A. CPU-only, offline-capable (the model lives in
# the liodon-engine container, not here).
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
boto3>=1.35,<2
PyMySQL>=1.1,<2
