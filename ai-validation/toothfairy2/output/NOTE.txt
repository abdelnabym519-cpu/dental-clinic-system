A real run writes here:

    input_provenance.json         what the input volume was (dims, spacing, SHA-256)
    checkpoint_verification.json  what the checkpoint file is (size, SHA-256)
    run_report.json               inference evidence: timing, RAM, labels, output hash
    run/                          the segmentation produced by nnU-Net

These are git-ignored: they are regenerated by every run, they can contain
patient-derived data, and keeping them out means `git status` stays clean after
you validate locally.

There is no output here yet. This lab has not run inference — the official
dataset requires an account and no public-access CBCT exists. See ../README.md.
